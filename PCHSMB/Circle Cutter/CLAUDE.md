# Claude Code Instructions — Circle Cutter

This subproject delegates to [AGENTS.md](AGENTS.md).

- **Tooling Invariant:** 100% pure Python standard library CAD generator (`generate_circle_cutter.py`). **Do NOT use OpenSCAD, FreeCAD, or external CAD binaries.**
- **Lifecycle:** Idea (`IDEAS.md`) $\to$ Plan (`Plans/`) $\to$ Implementation (`generate_circle_cutter.py`) $\to$ Commit.
- **Run Pipeline:** `python3 generate_circle_cutter.py`
